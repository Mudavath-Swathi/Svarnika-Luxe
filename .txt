# ML Features
1. Diamond Price Estimator (Linear/Random Forest)
2. Smart Recommendation System ( Rule-based + simple clustering)
3. Trending Diamonds Predictor (Time-series or weighted scoring)/ (Even basic math counts as ML here)
4. User Preferences Classifier (Classification/Logistic/Naive Bayes)
5. Fake vs Real Diamond Education ML

# Svarnika Diamonds - Modern Luxury Crafted to Perfection.

eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjY5NzBlYWYwNzI3MjQzMTUxYzhlMGNlYiIsImlhdCI6MTc2OTAwNzg1NywiZXhwIjoxNzY5NjEyNjU3fQ.U65ELJKs5xhQ7LNCUYFqVUrvU51WalOAjcWVowGBsy8

import { motion } from "framer-motion";
import { ShoppingBag } from "lucide-react";
import { useNavigate } from "react-router-dom";
import { useCart } from "../context/CartContext";

const ProductCard = ({ product }) => {
  const navigate = useNavigate();
  const { addToCart } = useCart();

  console.log("PRODUCT RECEIVED" , product)

  return (
    <motion.div
      whileHover={{ y: -6 }}
      transition={{ duration: 0.3, ease: "easeOut" }}
      onClick={() => navigate(`/product/${product.id}`)}
      className="group cursor-pointer"
    >
      {/* Image Card */}
      <div className="relative aspect-square rounded-2xl bg-black overflow-hidden">

        <img
          src={product.image}
          alt={product.name}
          className="w-full h-full object-contain transition-transform duration-500 group-hover:scale-105"
        />

        {/* Add to Cart */}
        <button
          type="button"
          onClick={(e) => {
            e.stopPropagation();
            addToCart(product);
            //navigate("/cart");
          }}
          className="absolute bottom-4 right-4 bg-white text-black p-3 rounded-full
                     opacity-0 group-hover:opacity-100 transition hover:scale-110"
        >
          <ShoppingBag size={18} />
        </button>
      </div>

      {/* Content */}
      <div className="mt-4 space-y-1">
        <h3 className="text-white text-sm font-medium tracking-wide">
          {product.name}
        </h3>

        <p className="text-xs text-gray-400 line-clamp-1">
          {product.shortDescription || "Crafted with timeless elegance"}
        </p>

        <p className="text-sm text-white font-semibold">
          ₹{product.price.toLocaleString("en-IN")}
        </p>
      </div>
    </motion.div>
  );
};

export default ProductCard;


import { useState } from "react";
import { NavLink, useNavigate } from "react-router-dom";
import { ShoppingBag, Search, User, Menu, X} from "lucide-react";
import {assets} from "../assets/assets";
import { useCart } from "../context/CartContext";

const Navbar = () => {
  const [open, setOpen] = useState(false);

  const navigate = useNavigate();

  const { cartItems } = useCart()

  const navLinks = [
    { name: "Home", path: "/" },
    { name: "Shop", path: "/shop" },
    { name: "About", path: "/about" },
    { name: "Contact", path: "/contact" },
  ];

  return (
    <>
      {/* Top Navbar */}
      <header className="w-full border-b border-[var(--border-subtle)]">
        <nav className="max-w-7xl mx-auto px-5 h-18 flex items-center justify-between">

          {/* Logo */}
          <NavLink to="/">
            <img src={assets.logo} alt="Svarnika Luxe" className="h-12 brightness-150 contrast-150"/>
          </NavLink>

          {/* Desktop links */}
          <ul className="hidden md:flex gap-10">
            {navLinks.map(link => (
              <NavLink key={link.name} to={link.path} className="text-[15px] text-[var(--text-secondary)] hover:text-[var(--text-primary)]">
                {link.name}
              </NavLink>
            ))}
          </ul>

          {/* Icons */}
          
            {/* Icons */}
<div className="flex items-center gap-5">

  <Search className="w-5 h-5 cursor-pointer" />

  {/* CART ICON WITH COUNT */}
  <div
    className="relative cursor-pointer"
    onClick={() => navigate("/cart")}
  >
    <ShoppingBag className="w-5 h-5" />

    {cartItems.length > 0 && (
      <span
        className="absolute -top-2 -right-2 bg-white text-black
                   text-xs px-1.5 rounded-full"
      >
        {cartItems.length}
      </span>
    )}
  </div>

  <User className="w-5 h-5 cursor-pointer" />


            

            {/* Mobile menu button */}
            <button className="md:hidden" onClick={() => setOpen(true)}>
              <Menu className="w-6 h-6" />
            </button>
          </div>
        </nav>
      </header>

      {/* FULL SCREEN MOBILE MENU */}
      {open && (
      <div className="fixed inset-0 z-50 bg-[var(--bg-main)] text-[var(--text-primary)] animate-fadeIn">

      {/* Close button */}
      <button onClick={() => setOpen(false)} className="absolute top-6 right-6">
      <X className="w-7 h-7 text-[var(--text-primary)]" />
    </button>

    {/* Centered menu links */}
    <div className="flex h-full flex-col items-center justify-center gap-10">
      {navLinks.map(link => (
        <NavLink key={link.name} to={link.path} onClick={() => setOpen(false)}
          className="text-2xl font-serif tracking-wide text-[var(--text-primary)] hover:opacity-80">
          {link.name}
        </NavLink>
      ))}
    </div>
</div>
)}
    </>
  );
};

export default Navbar;




eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjY5NzM3Njk1MTE1ZmNhZGM1OGM5YzJkNiIsImlhdCI6MTc2OTE3NDY3OCwiZXhwIjoxNzY5Nzc5NDc4fQ.bGPEZB9fi7t-EwsvslTQhITHu-zCKTHQHt-DOMkwLPU

eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjY5NzM3Njk1MTE1ZmNhZGM1OGM5YzJkNiIsImlhdCI6MTc2OTE3NDgyMSwiZXhwIjoxNzY5Nzc5NjIxfQ.00GL6Xa-wSLCo2cB7B0MwOXaSHhCrsJEdOavDca2Nkg






eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjY5NzM3Njk1MTE1ZmNhZGM1OGM5YzJkNiIsImlhdCI6MTc2OTE3NjYxNywiZXhwIjoxNzY5NzgxNDE3fQ.QDP1gyGa_DdigqO7X2H8uJrQiRPHanRP-ibBoMWsjxo